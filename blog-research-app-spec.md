# ブログリサーチアプリ 仕様書

## 1. 概要

### 1.1 アプリ名
**Blog Heading Researcher**

### 1.2 目的
指定したキーワードでGoogle検索を行い、上位10件のブログ記事から見出し構造（h1〜h4）を自動抽出する。さらに、記事間で共通するキーワードや見出しパターンを検出し、色分けして可視化することで、SEOライティングやコンテンツ企画を支援する。

### 1.3 対象ユーザー
- ブログライター / コンテンツマーケター
- SEO担当者
- アフィリエイター

---

## 2. 技術スタック

| 項目 | 技術 |
|------|------|
| バックエンド | Python 3.11+ / FastAPI |
| テンプレートエンジン | Jinja2 |
| Google検索 | Google Custom Search JSON API |
| スクレイピング | requests + BeautifulSoup4 |
| 共通キーワード分析 | 形態素解析（MeCab / janome） |
| スタイリング | Tailwind CSS (CDN) |
| 非同期処理 | httpx（非同期HTTPクライアント） |

---

## 3. 機能要件

### 3.1 キーワード検索機能

| 項目 | 内容 |
|------|------|
| 入力 | 検索キーワード（テキストフィールド） |
| 処理 | Google Custom Search APIで検索を実行 |
| 対象 | 検索結果の上位10件のURL |
| 制限 | 1回の検索で最大10件（API仕様に準拠） |

**APIリクエスト仕様:**
```
GET https://www.googleapis.com/customsearch/v1
  ?key={API_KEY}
  &cx={SEARCH_ENGINE_ID}
  &q={キーワード}
  &num=10
  &lr=lang_ja       # 日本語サイト優先（オプション）
```

### 3.2 見出し抽出機能

各記事のページにHTTPリクエストを送り、HTMLを取得。BeautifulSoup4で以下のタグを抽出する。

| 抽出対象タグ | 意味 |
|-------------|------|
| `<h1>` | 記事タイトル |
| `<h2>` | 大見出し |
| `<h3>` | 中見出し |
| `<h4>` | 小見出し |

**抽出ルール:**
- `<header>`, `<nav>`, `<footer>`, `<aside>`, `<sidebar>` 内の見出しは除外する（本文以外のナビゲーションや広告領域を除外）
- 見出しテキストの前後の空白をトリミングする
- 空の見出しタグは除外する
- 見出しの出現順序を保持する

**エラーハンドリング:**
- タイムアウト：1記事あたり最大10秒。超過した場合はスキップ
- ステータスコード 4xx / 5xx：該当記事をスキップし、エラーを表示
- robots.txt / JavaScript必須サイト：アクセスできない旨を表示

### 3.3 共通キーワード・見出し分析機能

10記事から抽出した見出しを横断的に分析し、共通パターンを検出する。

#### 3.3.1 分析手法

**A. 完全一致分析**
- 複数の記事で完全に同じ見出しテキストが存在する場合を検出

**B. 部分一致分析（形態素解析ベース）**
- 全見出しを形態素解析で分かち書きし、名詞・動詞・形容詞を抽出
- 3記事以上で出現する単語を「共通キーワード」として判定
- 出現頻度をカウントし、頻度順にランキング表示

**C. 見出し構造パターン分析**
- 「〇〇とは」「〇〇の方法」「〇〇のメリット・デメリット」など、よく使われる見出しパターンを検出

**D. 意味的類似グループ分析（シノニムグルーピング）**

文字が異なっていても意味が同じ・類似する表現を同一グループとしてまとめてカウントする。

例：「SEO対策の**ポイント**」と「SEO対策の**コツ**」→ 同じグループとして合算カウント

**実装方式：シノニム辞書 + パターンマッチング**

あらかじめ定義したシノニム（類義語）辞書を使い、見出し末尾のパターンを正規化してからカウントする。

```python
# シノニム辞書の例（synonym_groups.py）
SYNONYM_GROUPS = {
    "方法・やり方": ["方法", "やり方", "仕方", "手法", "手順", "ステップ", "進め方", "流れ"],
    "コツ・ポイント": ["コツ", "ポイント", "秘訣", "テクニック", "裏技", "裏ワザ", "ヒント", "Tips"],
    "メリット": ["メリット", "利点", "長所", "良い点", "強み", "魅力"],
    "デメリット": ["デメリット", "欠点", "短所", "弱点", "注意点", "落とし穴", "リスク"],
    "おすすめ": ["おすすめ", "オススメ", "推奨", "人気", "厳選", "選び方"],
    "比較": ["比較", "違い", "差", "vs", "VS", "対決"],
    "原因・理由": ["原因", "理由", "なぜ", "要因", "背景"],
    "対策・解決": ["対策", "解決", "解決策", "改善", "対処", "対応"],
    "基礎・基本": ["基礎", "基本", "入門", "初心者", "はじめて", "始め方", "初歩"],
    "まとめ": ["まとめ", "総括", "結論", "最後に", "おわりに", "終わりに"],
    "費用・料金": ["費用", "料金", "価格", "コスト", "相場", "値段"],
    "事例・具体例": ["事例", "具体例", "実例", "ケース", "成功事例", "活用例"],
}
```

**分析フロー:**

1. 各見出しテキストに対し、シノニム辞書の各語をスキャン
2. マッチした語を「グループ代表名」に正規化（例：「コツ」→「コツ・ポイント」）
3. 正規化後の語でカウントを集計
4. 結果表示時、同一グループに属する異なる表現をまとめて表示

**表示例:**

```
🔵 類似グループ：「コツ・ポイント」（7記事で出現）
   ├─ 記事1: 「SEO対策のポイント」
   ├─ 記事3: 「成功するコツ」
   ├─ 記事5: 「上位表示のテクニック」
   └─ 記事8: 「覚えておきたい秘訣」
```

**シノニム辞書の拡張:**
- 辞書はPythonファイル（`synonym_groups.py`）で管理し、ユーザーが自由に追加・編集可能
- 将来的にはUIから辞書を編集できる画面を追加することも検討

#### 3.3.2 色分けルール

| 出現頻度 | 色 | カラーコード | 意味 |
|---------|-----|------------|------|
| 8〜10記事で出現 | 赤 | `#EF4444` / `bg-red-200` | ほぼ必須のキーワード |
| 5〜7記事で出現 | オレンジ | `#F97316` / `bg-orange-200` | 重要度が高いキーワード |
| 3〜4記事で出現 | 黄 | `#EAB308` / `bg-yellow-200` | 一定の重要度があるキーワード |
| 1〜2記事のみ | 色なし | — | 独自性が高い（共通ではない） |

- 見出しテキスト内の該当キーワード部分をハイライト（`<mark>`タグ + 背景色）で表示する
- 色の凡例（レジェンド）を結果画面上部に常時表示する

**類似グループのハイライト:**
- 同一シノニムグループに属する語は、同じ色の下線（`border-bottom`）で表示する
- グループごとに固定の識別色（青系のグラデーション）を割り当て、どの語が同グループかを視覚的に識別できるようにする
- ツールチップ（hover時）でグループ名と他の類義語を表示する

---

## 4. 画面設計

### 4.1 画面一覧

| 画面 | URL | 説明 |
|------|-----|------|
| 検索画面（トップ） | `/` | キーワード入力 + 検索実行 |
| 結果画面 | `/results` | 見出し一覧 + 共通分析結果 |

### 4.2 検索画面（トップページ）

```
┌─────────────────────────────────────────────┐
│            Blog Heading Researcher           │
│                                              │
│  ┌───────────────────────────┐  ┌────────┐  │
│  │ キーワードを入力...        │  │ 検索   │  │
│  └───────────────────────────┘  └────────┘  │
│                                              │
│  ☑ 日本語サイトを優先する                      │
│                                              │
└─────────────────────────────────────────────┘
```

**要素:**
- キーワード入力フィールド（テキスト / 必須）
- 検索ボタン
- 日本語サイト優先チェックボックス（オプション）

### 4.3 結果画面

```
┌─────────────────────────────────────────────────────────┐
│  検索キーワード：「SEO 対策」        [新しい検索]         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ■ 色の凡例                                             │
│  ██ 赤：8〜10記事で出現  ██ オレンジ：5〜7記事で出現      │
│  ██ 黄：3〜4記事で出現                                   │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ▼ 共通キーワード ランキング                              │
│  ┌─────────────────────────────────────────────┐        │
│  │ 1. SEO対策（10記事） ██                      │        │
│  │ 2. キーワード選定（8記事） ██                 │        │
│  │ 3. 内部リンク（6記事） ██                     │        │
│  │ 4. コンテンツ（5記事） ██                     │        │
│  │ ...                                         │        │
│  └─────────────────────────────────────────────┘        │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ▼ 類似グループ分析                                      │
│  ┌─────────────────────────────────────────────┐        │
│  │ 🔵「コツ・ポイント」（7記事で出現）           │        │
│  │   記事1: SEO対策の[ポイント]                  │        │
│  │   記事3: 成功する[コツ]                       │        │
│  │   記事5: 上位表示の[テクニック]               │        │
│  │                                              │        │
│  │ 🟢「方法・やり方」（6記事で出現）             │        │
│  │   記事2: キーワード選定の[方法]               │        │
│  │   記事4: SEOの[やり方]                        │        │
│  │   記事7: 対策の[手順]                         │        │
│  │ ...                                          │        │
│  └─────────────────────────────────────────────┘        │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ▼ 記事別 見出し一覧                                     │
│                                                         │
│  ┌─ 記事 1：SEO対策の完全ガイド ────────────────┐        │
│  │  URL: https://example.com/seo-guide          │        │
│  │                                               │        │
│  │  h1: SEO対策の完全ガイド【2026年最新版】       │        │
│  │  ├─ h2: SEO対策とは                           │        │
│  │  ├─ h2: [キーワード選定]のやり方               │        │
│  │  │   ├─ h3: ツールを使った選定方法             │        │
│  │  │   └─ h3: 競合分析のポイント                 │        │
│  │  ├─ h2: [内部リンク]の最適化                   │        │
│  │  │   └─ h3: リンク構造の設計                   │        │
│  │  └─ h2: まとめ                                │        │
│  └───────────────────────────────────────────────┘       │
│                                                         │
│  ┌─ 記事 2：... ────────────────────────────────┐       │
│  │  ...                                          │       │
│  └───────────────────────────────────────────────┘       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**結果画面の構成:**

1. **ヘッダー部** — 検索キーワード表示 + 新しい検索ボタン
2. **色の凡例** — 色分けルールの説明
3. **共通キーワード ランキング** — 頻出キーワードを出現数順に表示
4. **記事別 見出し一覧** — 各記事ごとに見出しをツリー構造で表示。共通キーワードをハイライト

---

## 5. API設計

### 5.1 エンドポイント一覧

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/` | 検索画面を表示 |
| POST | `/search` | 検索を実行し結果画面へリダイレクト |
| GET | `/results?q={keyword}` | 結果画面を表示 |
| GET | `/api/search?q={keyword}&lang_ja={bool}` | 検索 + 分析結果をJSONで返却（API用） |

### 5.2 APIレスポンス例（`/api/search`）

```json
{
  "keyword": "SEO 対策",
  "total_articles": 10,
  "common_keywords": [
    { "word": "SEO対策", "count": 10, "level": "high" },
    { "word": "キーワード選定", "count": 8, "level": "high" },
    { "word": "内部リンク", "count": 6, "level": "medium" },
    { "word": "コンテンツ", "count": 5, "level": "medium" },
    { "word": "メタタグ", "count": 3, "level": "low" }
  ],
  "synonym_groups": [
    {
      "group_name": "コツ・ポイント",
      "total_count": 7,
      "level": "medium",
      "matches": [
        { "article_rank": 1, "heading": "競合分析のポイント", "matched_word": "ポイント" },
        { "article_rank": 3, "heading": "成功するコツ", "matched_word": "コツ" },
        { "article_rank": 5, "heading": "上位表示のテクニック", "matched_word": "テクニック" }
      ]
    },
    {
      "group_name": "方法・やり方",
      "total_count": 6,
      "level": "medium",
      "matches": [
        { "article_rank": 2, "heading": "キーワード選定の方法", "matched_word": "方法" },
        { "article_rank": 4, "heading": "SEOのやり方", "matched_word": "やり方" }
      ]
    }
  ],
  "articles": [
    {
      "rank": 1,
      "title": "SEO対策の完全ガイド【2026年最新版】",
      "url": "https://example.com/seo-guide",
      "headings": [
        { "tag": "h1", "text": "SEO対策の完全ガイド【2026年最新版】", "depth": 0 },
        { "tag": "h2", "text": "SEO対策とは", "depth": 1 },
        { "tag": "h2", "text": "キーワード選定のやり方", "depth": 1 },
        { "tag": "h3", "text": "ツールを使った選定方法", "depth": 2 },
        { "tag": "h3", "text": "競合分析のポイント", "depth": 2 },
        { "tag": "h2", "text": "内部リンクの最適化", "depth": 1 },
        { "tag": "h3", "text": "リンク構造の設計", "depth": 2 },
        { "tag": "h2", "text": "まとめ", "depth": 1 }
      ],
      "highlighted_keywords": ["SEO対策", "キーワード選定", "内部リンク"]
    }
  ]
}
```

---

## 6. ディレクトリ構成

```
blog-research-app/
├── main.py                  # FastAPIアプリのエントリーポイント
├── requirements.txt         # Python依存パッケージ
├── .env                     # 環境変数（APIキーなど）
├── .env.example             # .envのテンプレート
├── .gitignore
│
├── app/
│   ├── __init__.py
│   ├── config.py            # 設定管理（環境変数読み込み）
│   ├── routes.py            # ルーティング定義
│   │
│   ├── services/
│   │   ├── __init__.py
│   │   ├── google_search.py   # Google Custom Search API連携
│   │   ├── scraper.py         # Webスクレイピング（見出し抽出）
│   │   ├── analyzer.py        # 共通キーワード分析
│   │   └── synonym_groups.py  # シノニム（類義語）辞書定義
│   │
│   └── templates/
│       ├── base.html        # ベーステンプレート
│       ├── index.html       # 検索画面
│       └── results.html     # 結果画面
│
└── tests/
    ├── __init__.py
    ├── test_google_search.py
    ├── test_scraper.py
    └── test_analyzer.py
```

---

## 7. 処理フロー

```
ユーザー
  │
  │ 1. キーワードを入力して検索ボタンをクリック
  ▼
[POST /search]
  │
  │ 2. Google Custom Search APIにリクエスト
  ▼
[Google Custom Search API]
  │
  │ 3. 上位10件のURLリストを取得
  ▼
[スクレイピング処理] ← 10件を非同期で並列処理（httpx + asyncio）
  │
  │ 4. 各記事のHTMLを取得し、h1〜h4を抽出
  ▼
[共通分析処理]
  │
  │ 5. 形態素解析で全見出しからキーワードを抽出
  │ 6. 出現頻度をカウントし、共通キーワードを判定
  │ 7. 色分けレベルを付与
  ▼
[結果画面を描画]
  │
  │ 8. Jinja2テンプレートで結果をHTML化して返却
  ▼
ユーザー（結果を確認）
```

---

## 8. 環境変数

| 変数名 | 説明 | 例 |
|--------|------|-----|
| `GOOGLE_API_KEY` | Google Cloud APIキー | `AIzaSy...` |
| `GOOGLE_CSE_ID` | Custom Search Engine ID | `017576...` |
| `REQUEST_TIMEOUT` | 1記事あたりのタイムアウト（秒） | `10` |
| `MAX_ARTICLES` | 取得する記事数の上限 | `10` |

---

## 9. 依存パッケージ（requirements.txt）

```
fastapi>=0.104.0
uvicorn>=0.24.0
jinja2>=3.1.0
httpx>=0.25.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
janome>=0.5.0
```

| パッケージ | 用途 |
|-----------|------|
| fastapi | Webフレームワーク |
| uvicorn | ASGIサーバー |
| jinja2 | テンプレートエンジン |
| httpx | 非同期HTTPクライアント（スクレイピング用） |
| beautifulsoup4 | HTML解析（見出し抽出） |
| python-dotenv | .envファイル読み込み |
| janome | 日本語形態素解析（MeCab不要で導入が簡単） |

---

## 10. セットアップ手順

### 10.1 Google Custom Search APIの準備

1. [Google Cloud Console](https://console.cloud.google.com/) でプロジェクトを作成
2. 「Custom Search API」を有効化
3. APIキーを発行
4. [Programmable Search Engine](https://programmablesearchengine.google.com/) でカスタム検索エンジンを作成
   - 「ウェブ全体を検索」を有効にする
5. 検索エンジンID（cx）を取得

### 10.2 アプリの起動

```bash
# リポジトリをクローン
git clone <repository-url>
cd blog-research-app

# 仮想環境を作成
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 依存パッケージをインストール
pip install -r requirements.txt

# 環境変数を設定
cp .env.example .env
# .envを編集してAPIキーとCSE IDを設定

# アプリを起動
uvicorn main:app --reload --port 8000
```

ブラウザで `http://localhost:8000` にアクセスして利用開始。

---

## 11. 制約事項・注意点

| 項目 | 内容 |
|------|------|
| API利用制限 | Google Custom Search APIの無料枠は1日100リクエスト。それ以上は$5/1000リクエスト |
| スクレイピング制限 | robots.txtで禁止されているサイトはスキップする |
| JavaScript描画サイト | SPA等、JSで描画されるサイトの見出しは取得できない場合がある |
| 形態素解析精度 | janomeは辞書に依存するため、専門用語の分割精度に限界がある |
| レート制限 | スクレイピング先への負荷を考慮し、リクエスト間に0.5秒のインターバルを設ける（非同期並列数を最大5に制限） |

---

## 12. 今後の拡張案（スコープ外）

以下は初期リリースには含めないが、将来的に追加を検討する機能。

| 機能 | 説明 |
|------|------|
| 検索履歴の保存 | SQLiteで過去の検索結果を保存・閲覧 |
| CSV/Excelエクスポート | 見出し一覧をファイルとしてダウンロード |
| 見出し構成テンプレート生成 | 分析結果をもとに最適な見出し構成を自動提案 |
| 競合比較機能 | 自分の記事の見出しを入力し、上位記事との差分を表示 |
| 文字数カウント | 各記事の本文文字数を取得して比較 |
